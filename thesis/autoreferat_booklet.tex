% !TEX program = pdflatex
%
% Раскладка автореферата «книжкой» (для печати и фальцовки).
% Берёт готовый autoreferat.pdf (страницы A5) и раскладывает их по 2 на лист
% A4 в альбомной ориентации, В ПОРЯДКЕ ФАЛЬЦОВКИ (booklet imposition) —
% это и есть та «настройка в латехе»: опция signature пакета pdfpages.
%
% Порядок действий:
%   1) pdflatex autoreferat            (сначала собрать содержание, A5)
%   2) pdflatex autoreferat_booklet    (затем разложить книжкой, A4 альбом)
%
% Печать: на принтере выбрать двустороннюю печать «переворот по короткой
% стороне» (flip on short edge), затем сложить пополам и скрепить по сгибу.
%
% signature = число страниц в одной тетради, кратное 4 и не меньше объёма
% автореферата. Сейчас в автореферате 6 страниц -> signature=8 (один лист
% складывается из двух A4; две последние страницы остаются пустыми).
% Если объём изменится, поставьте ближайшее кратное 4, большее числа страниц.
%
\documentclass[a4paper]{article}
\usepackage{pdfpages}
\pagestyle{empty}

\begin{document}
\includepdf[pages=-,signature=8,landscape]{autoreferat.pdf}
\end{document}
